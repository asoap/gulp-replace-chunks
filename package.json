{
  "name": "gulp-replace-chunks",
  "version": "0.0.6",
  "description": "Replace chunks of code using files which are configured in the block",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "gulp",
    "replace"
  ],
  "author": "Derek <derek@caffeinedreams.ca>",
  "license": "ISC",
  "dependencies": {
    "ansi-colors": "^3.2.4",
    "event-stream": "^4.0.1",
    "plugin-error": "^1.0.1"
  }
}
